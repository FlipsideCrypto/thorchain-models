version: 2
models:
  - name: core__fact_validator_request_leave_events
    description: "Deprecating soon: This is a notice that we're replacing this table with gov.fact_validator_request_leave_events. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_VALIDATOR_REQUEST_LEAVE_EVENTS_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: TX_ID
        description: "{{ doc('tx_id') }}"
      - name: FROM_ADDRESS
        description: "{{ doc('from_address') }}"
      - name: NODE_ADDRESS
        description: "Address of node operator"
