version: 2
models:
  - name: core__fact_prices
    description: "Deprecating soon: This is a notice that we're replacing this table with price.fact_prices. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_PRICES_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: PRICE_RUNE_ASSET
        description: "How many RUNE can be exchanged by 1 Asset"
      - name: PRICE_ASSET_RUNE
        description: "How many Asset can be exchanged by 1 RUNE"
      - name: ASSET_USD
        description: "The Asset price in USD"
      - name: RUNE_USD
        description: "The Rune price in USD"
      - name: POOL_NAME
        description: "{{ doc('pool_name') }}"