version: 2
models:
  - name: core__fact_update_node_account_status_events
    description: "Deprecating soon: This is a notice that we're replacing this table with defi.fact_update_node_account_status_events. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_UPDATE_NODE_ACCOUNT_STATUS_EVENTS_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: FORMER_STATUS
        description: "The amount of rune for this add event"
      - name: CURRENT_STATUS
        description: "{{ doc('blockchain') }}"
      - name: NODE_ADDRESS
        description: "Address of node operator"