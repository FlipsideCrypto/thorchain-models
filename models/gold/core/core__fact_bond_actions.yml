version: 2
models:
  - name: core__fact_bond_actions
    description: "Deprecating soon: This is a notice that we're replacing this table with defi.fact_bond_actions. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_BOND_ACTIONS_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: TX_ID
        description: "{{ doc('tx_id') }}"
      - name: BLOCKCHAIN
        description: "{{ doc('blockchain') }}"
      - name: FROM_ADDRESS
        description: "{{ doc('from_address') }}"
      - name: TO_ADDRESS
        description: "{{ doc('to_address') }}"
      - name: ASSET
        description: "Same as pool name, which pool this bond events happens"
      - name: bond_type
        description: "{{ doc('bond_type') }}"
      - name: ASSET_AMOUNT
        description: "The asset amount for this bond event"
      - name: ASSET_USD
        description: "Used the price table to calculate the asset in the usd"
      - name: MEMO
        description: "{{ doc('memo') }}"