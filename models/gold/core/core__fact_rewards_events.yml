version: 2
models:
  - name: core__fact_rewards_events
    description: "Deprecating soon: This is a notice that we're replacing this table with defi.fact_rewards_events. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_REWARDS_EVENTS_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: BOND_E8
        description: "The rune amount of the bond for this pool at this block"