version: 2
models:
  - name: core__fact_rewards_event_entries
    description: "Deprecating soon: This is a notice that we're replacing this table with defi.fact_rewards_event_entries. Please migrate queries to the new table by 9/11/23. There will be no column changes or any other changes of any kind."
    columns:
      - name: FACT_REWARDS_EVENT_ENTRIES_ID
        description: "{{ doc('sk') }}"
      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"
      - name: DIM_BLOCK_ID
        description: "FK to DIM_BLOCK table"
      - name: POOL_NAME
        description: "{{ doc('pool_name') }}"
      - name: RUNE_E8
        description: "The rune amount of the rewards for this pool at this block"
